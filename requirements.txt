# ASTRIX — backend + ML + telemetry simulator
# Python 3.11+

# --- API layer ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
websockets==14.1
httpx==0.28.1

# --- ML / anomaly detection ---
numpy==2.2.1
pandas==2.2.3
scikit-learn==1.6.0
joblib==1.4.2
scipy==1.15.0

# --- structured memory ---
SQLAlchemy==2.0.36
# Postgres driver — only needed when ASTRIX_DATABASE_URL points at Postgres.
# SQLite (the default) needs no driver.
psycopg[binary]==3.2.3

# --- agent reasoning ---
anthropic>=0.42.0

# --- config ---
PyYAML==6.0.2
python-dotenv>=1.0

# --- optional ---
# chromadb==0.5.23        # swap the JSON vector store for Chroma
# torch==2.5.1            # Phase-2 autoencoder detector

# --- tests ---
pytest==8.3.4
